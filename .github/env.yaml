name: test_environment
channels:
  - conda-forge
  - bioconda
  - anaconda
dependencies:
  - biobb_common ==5.2.2
  - godmd ==1.7
  - emboss
  # To avoid fail related to old CPU architecture of the self-host runner
  - numpy <2.4.0 
